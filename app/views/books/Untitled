<main>
  <div class="container px-5 px-md-2">
    <div class="row">
      <div class="col-md-3 mt-3 px-md-2">
        <h2>User info</h2>

        <%= image_tag @user.get_profile_image(100, 100) %>

        <table class="table">
          <tbody>
            <tr></tr>
            <tr>
              <td><strong>name</strong></td>
              <td><strong><%= @user.name %></strong></td>
            </tr>
            <tr>
              <td><strong>introduction</strong></td>
              <td><strong><%= @user.introduction %></strong></td>
            </tr>
          </tbody>
        </table>

        <div class="row mt-5">
          <%= link_to edit_user_path(@user.id), class:"btn btn-outline-secondary btn-block" do %>
            <i class="fa-solid fa-user-gear"></i>
          <% end %>
        </div>

        <h2 class="mt-3">New book</h2>

        <%= form_with model: @book do |f| %>

          <label class="form-label">Title</label><br>
          <%= f.text_field :title, class:"form-control pb-0" %><br>

          <label class="form-label">Opinion</label><br>
          <%= f.text_area :body, class:"form-control pb-0" %><br>

          <%= f.submit "Create Book", class:"btn btn-success" %>
        <% end %>
      </div>

      <div class="col-md-8 mt-3 offset-1 px-2">
        <h2>Books</h2>

        <table class="table table-hover mb-5">
          <thead>
            <tr>
              <th></th>
              <th>Title</th>
              <th>Opinion</th>
              <th></th>
            </tr>
          </thead>
          <tbody>
            <% @books.each do |book| %>
              <tr>
                <td><%= image_tag @user.get_profile_image(80, 80) %></td>
                <td>
                  <%= link_to book_path(book.id) do %>
                    <p><%= book.title %></p>
                  <% end %>
                </td>
                <td><%= book.body %></td>
              </tr>
            <% end %>
          </tbody>
        </table>
      </div>
    </div>
  </div>
</main>
